package com.arenacode.arenacode.problem.domain;

/** PUBLIC: exemplo exibido ao jogador. PRIVATE: usado apenas pelo julgamento, nunca exposto. */
public enum TestCaseVisibility {
  PUBLIC,
  PRIVATE
}
